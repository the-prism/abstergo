@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

@if (Model.FolderID == -1)
{
    <div class="text-center">
        <h1 class="display-4">Pinned sites</h1>
        <p>To do ...</p>
    </div>
}

<div class="text-center">
    <h2>Favorites</h2>
</div>

@if (Model.CurrentFolder != null)
{
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a asp-page="/Index" asp-route-id="@Model.CurrentFolder.ParentID">Back</a></li>
            <li class="breadcrumb-item active" aria-current="page">@Model.CurrentFolder.Name</li>
        </ol>
    </nav>
}
else
{
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item active" aria-current="page">Home</li>
        </ol>
    </nav>
}

<table class="table">
    <thead>
        <tr>
            <th>
                ID
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Favorites[0].Url)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Favorites[0].IsFolder)
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Favorites)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Id)
                </td>
                <td>
                    @if (item.IsFolder)
                    {
                        <a asp-page="/Index" asp-route-id="@item.Id">@item.Name</a>
                    }
                    else
                    {
                        <a href="@item.Url">@Html.DisplayFor(modelItem => item.Name)</a>
                    }
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.IsFolder)
                </td>
            </tr>
        }
    </tbody>
</table>

<a class="btn btn-primary" asp-page="AddFavorite" asp-route-id="@Model.FolderID" asp-route-order="@Model.LastOrder" role="button">Add favorite</a>
<a class="btn btn-primary" asp-page="AddFolder" asp-route-id="@Model.FolderID" asp-route-order="@Model.LastOrder" role="button">New folder</a>